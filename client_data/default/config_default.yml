modules:
  load: "*"
  configuration:
    generators_mod:
      upath: generators@generators
services:
  load: "*"
  configuration:
    db:
      path: "@nxn/db/firestore.service"
      conPath: .firestore
      description: Firestore
