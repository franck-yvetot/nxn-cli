{
    "scripts": {
        "start": "node index.js",
        "pull": "git pull  && git submodule foreach --recursive git pull",
        "docker": "docker build . -f Dockerfile -t {MY_APP_NAME}",
        "docker:run": "docker run -p 3000:3000 {MY_APP_NAME}",
        "docker:login": "gcloud auth login",
    },
    "dependencies": {
        "@nxn/boot": "^1.0.58",
        "@nxn/db": "^1.0.81",
        "@nxn/files": "^1.0.23",
        "axios": "^1.6.7",
        "firebase-admin": "^12.0.0"
    }
}
